# Database Configuration
spring.datasource.url=jdbc:sqlite:src/main/resources/database/movies.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.sql.init.mode=always

# Hibernate Configuration (Actually no idea what's happening here, program had some errors that I don't remember and a user from StackOverflow had these settings)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.community.dialect.SQLiteDialect

